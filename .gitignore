# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets and local state
.env
.env.local
.env.*.local
.youtrack-aitrack-state/

# Beads / Dolt (local-only issue tracker; not shared via the public repo).
# Beads tracking history stays in your local .beads/ directory; if you want to
# adopt bd as your tracker in a fork, run `bd init` to create a fresh database.
.beads/
.dolt/
*.db
.beads-credential-key

# Claude Code per-user overrides (project settings.json is tracked)
.claude/settings.local.json
